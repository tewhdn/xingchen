{
  "version": 2,
  "builds": [
    {
      "src": "api/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/", "dest": "/public/index.html" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "env": {
-    "DEEPSEEK_API_KEY": "@deepseek-api-key"
+    "DEEPSEEK_API_KEY": "@DEEPSEEK_API_KEY"
  }
}
